<%@event.enrollments.each do |xa| %>	
	<%if xa.user_id==current_user.id%>
		<%@exist = 1 %>
	<%end%>
<%end%>
<%if @exist==1 %>
	<%= button_to "Cancel", { :controller => "enrollments", :action => "destroy", :event_id=>@event.id} %>
<%elsif @exist==0%>
	<%= button_to "Join", { :controller => "enrollments", :action => "new",:event_id=>@event.id} %>	
<%end%>