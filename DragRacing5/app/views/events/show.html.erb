<br>
<br>
<script src="//maps.google.com/maps/api/js?v=3.22&client=&key=&libraries=geometry&language=&hl=&region="></script> 
<script src="//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/infobox/1.1.9/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->
<%=render "navbar"%>
	<center><%= image_tag @event.avatar.url(:original), :style=>"width:1300px; height: 350px;"%></center>
<p id="notice"><%= notice %></p>
<div class="eventpage">
<ul class="list-group">
  <li class="list-group-item list-group-item-default" style='width:300px;'><big><big><big><span class="label label-primary">Title</span></big></big></big>
<%=@event.title%>
  <li class="list-group-item list-group-item-default" style='width:300px'><big><big><big><span class="label label-success">Description</span></big></big></big>
<big><span class="label label-info"><div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-body">
    <p><%=@event.description%></p>
  </div></span></big><br></li>
</ul>
<!-- <div class="enroll">
<%= link_to(content_tag(:span, @event.enrollments.size , class:"badge")+"Enrollments",{:controller => "enrollments", :action => "index", :id => @event.id}, class: 'btn btn-primary')%> 
 </div> -->
 <!--  <%=Event.first%>
  <%=User.first.email%> -->
</p>
<table class="eventsbuttons">
	<tbody>
	<%@exist=0%>
	<%if user_signed_in?%>
		<%@event.enrollments.each do |xa| %>	
			<%if xa.user_id==current_user.id%>
				<%@exist = 1 %>
				<%@enrollment=xa%>
			<%end%>
		<%end%>
		<tbody>
		<tr>
		<td>
		<%if @exist==1 %>
			<%= button_to "Cancel", { :controller => "enrollments", :action => "destroy",:enrollment_id => @enrollment.id},method: :delete,class: 'btn btn-danger' %>
		<%elsif @exist==0%>
			<%= button_to "Join", { :controller => "enrollments", :action => "new",:event_id => @event.id},class: 'btn btn-primary' %>
		<%end%>
		</td>
	<%end%>
	<td>
	<%if user_signed_in? %>
	   <td><div class="likes"><%= render 'like' %></div></td>
			<td><%=link_to "Add comment",{ :controller => "comments", :action => "new", :event_id => @event.id},class: " glyphicon glyphicon-comment"%></td>
		<td><%=render_shareable :buttons=> ['twitter']%><td> 
	<%end%>
	</td>
	<td><ul class="pager"> <li class="previous"> <%= link_to(content_tag(:rel, "Comments" , href:"animate.min.css"),{:controller => "comments", :action =>"index" ,:event_id => @event.id}, class: ' animated slideInDown') %></li></td>
	</ul>
	</tr>
	</tbody>
</table>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <li class="navbar-brand"><big><%=Event%></big></li>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <%if @onindex !="indexpage"%>
          <li class='active'><%= link_to(content_tag(:span, @event.enrollments.size , class:"badge")+"Enrollments",{:controller => "enrollments", :action => "index", :id => @event.id})%> </li>
        <%end%>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
          <li><%if user_signed_in? %>
	<%if @event.user_id==current_user.id%>
		<%if(Date.parse(@event.date.strftime('%Y-%m-%d %H:%M')) <= Date.today) %>
			<li><%=link_to "Add result",{:controller => "results", :action => "new" , :event_id => @event.id} %></li>
			<li><%=link_to "Results", {:controller=>"results", :action => "index" ,:event_id=>@event.id } %></li>
			<%end%>
			<%end%>
			<%end%>
          </ul>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->

</nav>
<table class="test">
<tbody>
<tr>
<td>
	<%if @event.video_file_name!=nil%>
		<%= video_tag @event.video.url(:medium), controls: true, type: "video/mp4", size: "400x400",style: "margin-left: 12%;" %>
	<%end%>
</td>
<td><div style='width: 500px;margin-left: 35%;'>
  <div id="map" style='width:500px; height: 400px;'></div>
</div>
</td>
</tr>
</tbody>
</table>
</div>
<script type="text/javascript">handler = Gmaps.build('Google');
handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
  markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
});
</script>
<ul class="pager">
    <li class="previous"> <%= link_to(content_tag(:rel, "Back" , href:"animate.min.css"),events_path, class: 'animated zoomInDown') %>
	</li><span aria-hidden="true"></span> </a></li>
</ul>